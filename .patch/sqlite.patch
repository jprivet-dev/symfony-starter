Subject: [PATCH] conf(compose.override.yaml): add var/data_dev.db in the volumes
feat(sqlite.patch): create new patch
---
Index: .patch/activate-sqlite.patch
===================================================================
diff --git a/.patch/activate-sqlite.patch b/.patch/activate-sqlite.patch
new file mode 100644
--- /dev/null	(revision 812fb684ad7f9e9cec8b86334e9a3aabca3989ed)
+++ b/.patch/activate-sqlite.patch	(revision 812fb684ad7f9e9cec8b86334e9a3aabca3989ed)
@@ -0,0 +1,37 @@
+Subject: [PATCH] feat(sqlite): activate sqlite (Dockerfile + compose.xxx.yaml)
+---
+Index: Dockerfile
+IDEA additional info:
+Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
+<+>UTF-8
+===================================================================
+diff --git a/Dockerfile b/Dockerfile
+--- a/Dockerfile	(revision d59015cd3ec55847013205f21b8d6c84ba0e11f4)
++++ b/Dockerfile	(revision ebcefd34006cb42979035f2e381d93288c62c9c1)
+@@ -42,6 +42,9 @@
+ ###> recipes ###
+ ###< recipes ###
+
++RUN apt-get update && apt-get install -y sqlite3 && rm -rf /var/lib/apt/lists/*
++RUN install-php-extensions pdo_sqlite
++
+ COPY --link frankenphp/conf.d/10-app.ini $PHP_INI_DIR/app.conf.d/
+ COPY --link --chmod=755 frankenphp/docker-entrypoint.sh /usr/local/bin/docker-entrypoint
+ COPY --link frankenphp/Caddyfile /etc/frankenphp/Caddyfile
+Index: compose.yaml
+IDEA additional info:
+Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
+<+>UTF-8
+===================================================================
+diff --git a/compose.yaml b/compose.yaml
+--- a/compose.yaml	(revision d59015cd3ec55847013205f21b8d6c84ba0e11f4)
++++ b/compose.yaml	(revision ebcefd34006cb42979035f2e381d93288c62c9c1)
+@@ -9,7 +9,7 @@
+       MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
+       MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
+       # Run "composer require symfony/orm-pack" to install and configure Doctrine ORM
+-      DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/${POSTGRES_DB:-app}?serverVersion=${POSTGRES_VERSION:-15}&charset=${POSTGRES_CHARSET:-utf8}
++      DATABASE_URL: ${DATABASE_URL}
+       # Run "composer require symfony/mercure-bundle" to install and configure the Mercure integration
+       MERCURE_URL: ${CADDY_MERCURE_URL:-http://php/.well-known/mercure}
+       MERCURE_PUBLIC_URL: ${CADDY_MERCURE_PUBLIC_URL:-https://${SERVER_NAME:-localhost}:${HTTPS_PORT:-443}/.well-known/mercure}
Index: compose.override.yaml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/compose.override.yaml b/compose.override.yaml
--- a/compose.override.yaml	(revision 6b482fabafa3dfcc25bfd07a3d2c3fb576181c00)
+++ b/compose.override.yaml	(revision 1620090a06ffa240049c81643ac3177a64f30e2e)
@@ -12,6 +12,7 @@
       # If you develop on Mac or Windows you can remove the vendor/ directory
       #  from the bind-mount for better performance by enabling the next line:
       #- /app/vendor
+      - ./var/data_dev.db:/app/var/data_dev.db:rw
     environment:
       FRANKENPHP_WORKER_CONFIG: watch
       MERCURE_EXTRA_DIRECTIVES: demo
